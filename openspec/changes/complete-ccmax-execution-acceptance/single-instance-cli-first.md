# 推进收敛：先交付一个真实CLI实例

2026-09-17，用户确认先单实例闭环，再双实例隔离，最后CCMAX接入。
覆盖此前S1d优先顺序：暂停新增镜像构建工具；保留未提交WIP，不将其计作完成。
已有基础镜像/固定CLI和Bun直接用于测试，不重新下载或替换宿主工具。

## 本轮可演示的交付

`HTTP调用方 → isthmus CLI适配服务 → 官方CLI2.1.258 → 容器内回环模拟上游 → 原路返回`

- `src/runtime/cli/`：固定命令与空白环境、stdin输入、子进程监督、stream-json事件。
- `src/app/cli/`：显式启动的回环HTTP服务，单实例单请求，取消/错误传递。
- `test/cli/`：纯合成上游、实际链路冒烟与负向测试；不把测试放进生产默认入口。
- `image/lab/cli.py`：复用既有受限容器与固定工具，仅上传本轮正向清单和测试。

第一版仅支持单轮用户文本、固定模型、JSON与SSE返回。拒绝工具/MCP/多轮/未知
控制参数，不静默假装兼容。fake服务原入口不变；此新入口明确cli-probe非生产。
真实CLI退出0与成功result之后才发最终成功结束；解析错误、超时、取消都回收子进程。

## 安全边界与验收

仅170测试机，43仅SSH中转；不访问生产、不用真实凭据、不改业务/数据库/UI。
使用原已核验基础image ID，UID1000、只读根、dropALL、NNP、networknone、无宿主挂载
和端口、独立tmpfs home、1CPU/2GiB无swap/pids128。CLI/服务/模拟上游在同一容器，
仅回环，随机capture路径，工具/项目配置/MCP/会话持久化关闭；没有真实上游client。
使用明确synthetic凭据，usage为合成值；不保留正文、认证头或CLI原始输出。

先纯模块和HTTP负例，再原生Linux真实CLI正常JSON/SSE闭环、取消与失败检查；
独立review、清理自有容器、比较原业务基线，最后提交并报告实际演示结果。
不因测试量涨分；完整R3、R4、R5按原门槛分别核对。当前26%仅为历史验收口径。

后两阶段：双实例独立home/身份/密钥/证书/出口拒绝矩阵；随后现有Sub2/CCMAX
接入，流式结果和usage回传，计费继续由Sub2负责。不把双架构和长期压测前置。
