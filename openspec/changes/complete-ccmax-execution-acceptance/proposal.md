# CCMAX 转发与隔离验收收尾

用户于 2026-09-16 要求先规划再继续完善至验收，且保持线上数据、UI、使用不受影响。

本变更衔接 `add-claude-execution-plane-v1`、DP1 和恢复合同，不复写其历史完成项。总体范围与阶段门槛见 [实施计划](../../../docs/plans/ccmax-execution-acceptance-v2.md)。

第一批解决真实 HTTP worker 的整包缓冲断点，并建立不改变原始正文的 usage 提取。不会将生产 gateway 切到新执行面，也不修改线上系统。后续按宿主装配、网关、CLI、凭据生命周期、完整本地 E2E 的依赖推进。

验收状态分本地工程、兼容证据、生产许可三个层次；没有真实 canary / 部署授权时不得把上线总门槛标为通过。
