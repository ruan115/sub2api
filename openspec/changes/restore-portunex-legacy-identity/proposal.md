# Portunex 旧 Bearer 认证恢复

用户于 2026-09-13 确认上一切片的认证规划，授权继续本地实现、验证和分阶段 Git 提交。
目标是旧 login → users/me → logout 闭环，保留现有调用方式和独立恢复域。
先补证据，再实现可证实行为；未知项不变成猜测的生产默认值。

不部署、不推送、不替换 Bun、不导出真实账号/会话，不改变原有演示和 Sub2API 认证。
独立数据库只允许合成数据；新增外部运行时安装另行确认。旧接口对照验证前不得宣称兼容。
